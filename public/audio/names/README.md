# Aussprache-Audio (optional)

Lege hier MP3/WAV ab und setze in `src/data/players.json` z. B.:

```json
"nameAudio": "/audio/names/nomi-in-de-braekt.mp3"
```

Solange keine Datei gesetzt ist, nutzt die Profilseite die Browser-Sprachausgabe (TTS).
